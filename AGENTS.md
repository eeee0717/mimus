# mimus

保留版面的 PDF 翻译 CLI（Rust）。开始任何工作前先读 `CONTEXT.md`（术语与全部已定决策）与相关 `docs/adr/`。

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`eeee0717/mimus`), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary; label strings equal the role names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
