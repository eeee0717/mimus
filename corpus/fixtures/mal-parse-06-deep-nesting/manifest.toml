schema_version = 1
[identity]
id = "mal-parse-06-deep-nesting"
name = "512-level nested content array"
class = "mal"
legality = "malformed"
cases = ["PARSE-08"]
priority = "M0"
variable = "The page Contents reference selects a stream containing 512 nested arrays"
[source]
method = "byte-mutation"
pdf = "mal-parse-06-deep-nesting.pdf"
pdf_sha256 = "1f0e2a5e8ee3672c8e194454b8e8a48306b44e4e592a047f58cb2bcefb445dbd"
font_provenance = "vendored"
fonts = [{ file = "../../fonts/MimusExact.ttf", sha256 = "6e1e40974dce5dca579f3f191dd7dcc9953e6e04165d69f36d01aa8242a24735" }]
[lineage]
parent = "unit-stream-00-malformed-parent"
[[lineage.mutations]]
byte_offset = 210
original_bytes = [48]
replacement_bytes = [54]
description = "change /Contents 10 0 R to 16 0 R, selecting the 512-level nested-array candidate"
[[page]]
index = 0
media_box = [0.0,0.0,300.0,200.0]
rotate = 0
[expected]
geometry_source = "hand-written"
tolerance_pt = 0.001
block = []
declared_failure = "operator-walk:nesting-too-deep-128"
renderer_diagnostic = "too many syntax errors; ignoring rest of page"
[expected.structure]
pages = 1
blocks = 0
columns = 1
[[expected.behaviour]]
id = "PARSE-08-depth-limit"
assertion = "The tokenizer stops at nesting level 129 with NestingTooDeep(limit=128) without panic, abort, or unbounded recursion"
observable_via = "single-byte lineage, bounded process exit, stable error payload and timeout watchdog"
[oracle]
checks = ["determinism","legality","operator-walk","page-geometry","render-diagnostic"]
[[adjudication]]
date = "2026-08-22"
issue = "Recursive descent over 512 arrays can overflow the Rust stack"
resolution = "Use an explicit stack and a hard global nesting limit of 128"
