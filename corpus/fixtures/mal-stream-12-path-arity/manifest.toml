schema_version = 1

[identity]
id = "mal-stream-12-path-arity"
name = "Path line operator has one coordinate"
class = "mal"
legality = "malformed"
cases = ["STREAM-03"]
priority = "M3"
variable = "The legal parent line segment loses only its second numeric operand through an equal-length whitespace replacement"

[source]
method = "byte-mutation"
pdf = "mal-stream-12-path-arity.pdf"
pdf_sha256 = "d2ef38ba598502a82dd46399c9726243e85cd29530ad7cbab114f185d7317f3a"
font_provenance = "vendored"
fonts = [{ file = "../../fonts/MimusExact.ttf", sha256 = "6e1e40974dce5dca579f3f191dd7dcc9953e6e04165d69f36d01aa8242a24735" }]

[lineage]
parent = "unit-stream-12-path-parent"

[[lineage.mutations]]
byte_offset = 3347
original_bytes = [48]
replacement_bytes = [32]
description = "replace the second l coordinate 0 with whitespace so the operator retains only operand 180"

[[page]]
index = 0
media_box = [0.0, 0.0, 300.0, 200.0]
rotate = 0

[expected]
geometry_source = "hand-written"
tolerance_pt = 0.001
declared_failure = "content-semantics:STREAM-03"
block = []

[[expected.degradation]]
scope = "page"
page = 0
reason = "graphics_unreliable"

[expected.structure]
pages = 1
blocks = 0
columns = 1

[[expected.behaviour]]
id = "STREAM-03-page-preservation"
assertion = "Malformed path arity marks graphics unreliable, produces a typed page degradation, and publishes the original page bytes without translating its otherwise valid text"
observable_via = "single-byte lineage, production page_degraded event, decoded content equality, and independent raster preservation"

[oracle]
checks = ["determinism", "legality", "page-geometry", "render"]
