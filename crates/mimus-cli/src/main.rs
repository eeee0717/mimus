//! `mimus` -- translate a PDF while preserving its layout.

fn main() -> anyhow::Result<()> {
    tracing::info!("mimus {}", env!("CARGO_PKG_VERSION"));
    eprintln!("mimus: nothing wired up yet. See README.md for the milestone order.");
    Ok(())
}
